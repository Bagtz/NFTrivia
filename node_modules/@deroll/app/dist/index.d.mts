import { AppOptions, App } from '@deroll/core';

type HttpAppOptions = AppOptions & {
    url: string;
};

declare const createApp: (options: HttpAppOptions) => App;

export { type HttpAppOptions as AppOptions, createApp };
